spring:
    application:
        name: eapw
    datasource:
        url: ${DATASOURCE_URL}
        username: postgres
    jpa:
        hibernate:
            ddl-auto: update
        show-sql: true
        open-in-view: false
    devtools:
        add-properties: false